Studies in which a design parameter is chosen against a simulated response or
a computed feasibility bound rather than prescribed.

Pulse design is the case where both appear at once: the profile a selective
pulse produces is available only by simulating it, and the pulses that can be
played at all are bounded by the gradient amplitude their selection needs.