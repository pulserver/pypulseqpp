# Constraint checks

What the package computes over a finished sequence, and what each check
reports beside its verdict. A passing check does not establish that a
sequence is safe to run on a scanner or on a subject.

| Page | What it establishes |
| --- | --- |
| {doc}`/generated/gallery/06-checks/01_constraint_checks` | Timing, gradient amplitude, slew rate, continuity, nerve stimulation, mechanical resonance and SAR over one echo planar sequence, with the reading each returns. |

```{toctree}
:hidden:

/generated/gallery/06-checks/01_constraint_checks
```
