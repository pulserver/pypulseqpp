# Fast spin echoes

One excitation followed by a CPMG train of refocusing pulses, with one view
acquired per echo. The order in which the train covers k-space decides the
weighting the image carries.

| Example | What it covers |
| --- | --- |
| {doc}`/generated/gallery/13-fast-spin-echo/fse3D_sequence` | One CPMG train per excitation, one (line, partition) view per echo. |
| {doc}`/generated/gallery/13-fast-spin-echo/fse-echo-ordering-and-point-spread` | The echo amplitudes of the train, the k-space weighting the ordering produces, and the width of the resulting point-spread function. |

```{toctree}
:hidden:

/generated/gallery/13-fast-spin-echo/fse3D_sequence
/generated/gallery/13-fast-spin-echo/fse-echo-ordering-and-point-spread
```
