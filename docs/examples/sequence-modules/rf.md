# RF and excitation modules

Modules built around one RF pulse and the gradients that select with it.

| Example | What it covers |
| --- | --- |
| {doc}`/generated/gallery/21-modules-rf/excitation-modules` | The excitation and refocusing modules the package ships, and the pulse each designs. |
| {doc}`/generated/gallery/21-modules-rf/slice-profile-and-time-bandwidth` | Time-bandwidth product, pulse duration and the slice profile a selective design produces, against the gradient amplitude the selection needs. |

```{toctree}
:hidden:

/generated/gallery/21-modules-rf/excitation-modules
/generated/gallery/21-modules-rf/slice-profile-and-time-bandwidth
```
