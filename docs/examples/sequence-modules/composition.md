# Composing modules

A sequence module solves the layout of one group of blocks at construction and
publishes the resulting events for a scan loop to place. The architecture is
described in {doc}`/explanations/design/sequence-module`.

| Example | Scope |
| --- | --- |
| {doc}`/generated/gallery/20-modules-overview/sequence-module-composition` | The blocks, the published events and the timing reference a module exposes, and the interval two modules compose at. |

```{toctree}
:hidden:

/generated/gallery/20-modules-overview/sequence-module-composition
```
