# Getting started

The minimal Pulseq workflow, written out event by event, and the checks a
finished sequence is put through before it is written.

| Example | What it covers |
| --- | --- |
| {doc}`/generated/gallery/01-getting-started/basic-pulseq-sequence` | A spoiled gradient echo built from events and blocks alone, checked and written to a `.seq` file. |
| {doc}`/generated/gallery/01-getting-started/safety-checks` | The gradient, stimulation, resonance and SAR checks over one sequence, with the diagnostics each reports. |

```{toctree}
:hidden:

/generated/gallery/01-getting-started/basic-pulseq-sequence
/generated/gallery/01-getting-started/safety-checks
```
