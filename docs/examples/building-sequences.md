# Building sequences

Assembling modules, a prescription and a scan loop into a sequence application:
the contract the shipped sequences and the command line are written against.

| Example | What it covers |
| --- | --- |
| {doc}`/generated/gallery/30-building-sequences/sequence-app-from-scratch` | A complete `SequenceApp` subclass: the prescription in `init_sequence`, the scan in `loop`, one repetition in `kernel`, and the definitions in `finalize`. |

```{toctree}
:hidden:

/generated/gallery/30-building-sequences/sequence-app-from-scratch
```
