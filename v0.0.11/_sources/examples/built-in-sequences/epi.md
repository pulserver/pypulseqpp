# Echo-planar imaging

One excitation followed by a train of readout lobes of alternating polarity
with phase-encode blips, so several phase-encode lines are acquired after each
excitation and off-resonance phase accumulates along the phase-encode axis.

| Example | Scope |
| --- | --- |
| {doc}`/generated/gallery/15-epi/epi2D_sequence` | One blipped echo train per excitation, with segmentation and in-plane acceleration compared by echo-train length and distortion. |
| {doc}`/generated/gallery/15-epi/epi3D_sequence` | The CAIPIRINHA lattice and its skipped-CAIPI traversal, single-shot and segmented. |

```{toctree}
:hidden:

/generated/gallery/15-epi/epi2D_sequence
/generated/gallery/15-epi/epi3D_sequence
```
