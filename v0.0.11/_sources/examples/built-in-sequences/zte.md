# Zero echo time

The readout gradient is already at amplitude when the pulse is transmitted, so
acquisition begins after the transmit/receive switching dead time.

| Example | Scope |
| --- | --- |
| {doc}`/generated/gallery/16-zte/zte3D_sequence` | A hard pulse under a readout gradient at amplitude, one radial half-spoke per view. |

```{toctree}
:hidden:

/generated/gallery/16-zte/zte3D_sequence
```
