# Balanced SSFP

Every gradient axis returns to zero net moment within each repetition, so
the transverse magnetisation is not spoiled and the signal reaches a steady
state that depends on T1 and T2.

| Example | Scope |
| --- | --- |
| {doc}`/generated/gallery/14-bssfp/bssfp2D_sequence` | One phase-encode line per repetition, every gradient axis balanced. |
| {doc}`/generated/gallery/14-bssfp/bssfp3D_sequence` | One (line, partition) view per repetition, every gradient axis balanced. |

```{toctree}
:hidden:

/generated/gallery/14-bssfp/bssfp2D_sequence
/generated/gallery/14-bssfp/bssfp3D_sequence
```
